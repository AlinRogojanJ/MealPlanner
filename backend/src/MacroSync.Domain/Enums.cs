namespace MacroSync.Domain;

public enum DietType { Cut, Maintain, Bulk }

public enum SlotType { Breakfast, Lunch, Dinner, Snack }

public enum HouseholdRole { Owner, Member }

public enum IndivisibleSplitRule { Even, Ratio, Assigned }

public enum FoodLogSource { Planned, OffPlan }

public enum SuggestionStatus { Pending, Accepted, Dismissed }
